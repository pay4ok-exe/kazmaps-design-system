# Что нужно от дизайнера

Макет: Figma «KazMaps Design System» (`eI51Nnu0d74Y5xdO3XIZ30`).
Список того, что нельзя было решить замером. Пока пункт открыт, в коде
стоит временное решение из колонки «сейчас».

| #   | нужно                                                                                                                                                                                                                                                                    | сейчас в коде                                                                                                                   |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Состояние hover кнопки.** `State=Hover` побайтово равен `State=Default` у всех трёх типов — совпадают заливка, оба стопа градиента и трансформация                                                                                                                     | hover-стиля нет, кнопка реагирует только на нажатие                                                                             |
| 2   | **Вариант `outline`.** В макете только `Accent`, `Neutral`, `Danger`. В main-web `outline` + `outline-accent` — 45 вызовов Button из 77                                                                                                                                  | прежняя геометрия на новых ролях                                                                                                |
| 3   | **Размеры кнопки кроме 40px.** `sm` (34) и `lg` (44) не ложатся даже на шкалу `dimension/height` макета: 28 → 40 → 64                                                                                                                                                    | произвольные значения                                                                                                           |
| 4   | **Составные тени кита** (`--shadow-button-md`, `--shadow-modal`, `--shadow-sheet-top`, `--shadow-dropdown`, `--shadow-column`): `numerics` даёт только `shadow/blur/*` и `shadow/position/*`, без цвета и альфы. Тени поля поиска и HUD уже измерены на нодах, эти — нет | тени прежнего кита                                                                                                              |
| 5   | **Длительности моушена и `--shimmer-peak`.** Переменных времени в макете нет                                                                                                                                                                                             | значения прежнего кита                                                                                                          |
| 6   | **Кегли крупнее 16px.** `typography/font-size` обрывается на 16                                                                                                                                                                                                          | `--text-lg` … `--text-3xl` прежние                                                                                              |
| 7   | **Свести `numerics` с вёрсткой.** Объявлено `font-size` 10/12/14/16 — используются 10, 12, 16 и **20**, кегль 14 не встречается. Объявлено `line-height` 12/16/18/20 — используются 12, 16, 20 и **24**, 18 не встречается                                               | роли заведены как объявлено                                                                                                     |
| 8   | **Опечатка `icon/dander`** (ожидается `icon/danger`)                                                                                                                                                                                                                     | имя как в источнике, `--icon-dander`                                                                                            |
| 9   | **Вес текста кнопки зависит от иконки:** без неё 500, с ней 450                                                                                                                                                                                                          | воспроизведено буквально                                                                                                        |
| 10  | **Нижний стоп обводки `danger`** — `action/danger/hover` (альфа 50%), тогда как у `accent` и `neutral` там `secondary`                                                                                                                                                   | воспроизведено буквально                                                                                                        |
| 11  | **Где живёт код страны в поле телефона.** В макете селектор показывает только флаг, а `+7` набрано в самом поле как текст (`text/primary`) поверх маски `000 000 00 00` (`text/tertiary`)                                                                                | код страны остался в селекторе: перенос его в поле — переделка маски, на которой стоит разбор казахстанских номеров с ведущей 8 |
| 12  | **Зазор между ячейками кода.** `149:836` — витрина четырёх состояний одной ячейки, собранного поля в макете нет                                                                                                                                                          | взята десятка, на которой стоят экраны входа                                                                                    |
| 13  | **У `Map Action` тип Default выбивается из ряда.** У `Locate` и `Dimension` иконка темнеет на наведении (`icon/tertiary` → `icon/primary`), у `Default` — наоборот светлеет (`icon/primary` → `icon/tertiary`)                                                           | воспроизведён порядок остальных восьми вариантов                                                                                |
| 14  | **Тени HUD не разведены по ролям.** Четыре измеренные тени различаются только смещением и альфой; какая где — выведено из того, на каком компоненте снята                                                                                                                | заведены `--shadow-hud`, `--shadow-hud-hover`, `--shadow-hud-side`, `--shadow-hud-badge`                                        |

## Почему временные решения именно такие

Там, где в макете значения нет, подставлено прежнее значение кита с
пометкой «ожидает значения от дизайнера» — сводный список собирает
`docs/tokens.md`. Подобранное «на глаз» число неотличимо от измеренного и
разъезжается с макетом молча, поэтому его не заводят.

Имена ролей не правятся даже при явной опечатке (п. 8): имя роли в коде
обязано совпадать с именем переменной в Figma, иначе рассыпается
трассировка, на которой держится `scripts/import-figma.mjs`.

Роли `highlight`/`highlight-soft` (и алиасы `gold`/`gold-soft`) не
переносили вовсе — в макете их нет, в main-web ссылок ноль.

## Ловушка при перепроверке

`fontName.style` у веса 450 — тоже `"Regular"`, как и у 400. Отличить
можно только по `fontWeight`. Гарнитура и веса переменными не заданы: это
свойства текстовых нод, снимаются обходом текстов страницы.
